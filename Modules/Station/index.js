


var Station = require ("http").createServer ((req, res) => {
	const { url, method } = req;
	if (url === "/") {


	}
});

Station.listen (() => {
	const { port, address, family } = Station.address();
	console.log (`Station ready at ${ address }:${ port }`);


	Resolve ();
});
